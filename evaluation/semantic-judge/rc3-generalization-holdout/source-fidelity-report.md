# Source Fidelity Report

Automatic verification: every evidence span in every public case is a
byte-exact excerpt of the referenced KB file (kb_ref resolved in the
repository; kb/ prefix is the logical namespace, files live at the repo
root).

- Spans checked: 233 across 159 CORE cases. Mismatches: 0.
- No rewritten or paraphrased evidence spans are present.
- KB was treated read-only for the whole task (QA: no KB file modified
  within 24h window at check time).
