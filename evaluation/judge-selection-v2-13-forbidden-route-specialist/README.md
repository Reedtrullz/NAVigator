# NAV-EXPLORE-JUDGE-SELECTION-V2_13-FORBIDDEN-ROUTE-SPECIALIST

AFK Measurement-System Campaign, Stage 2D. Specialized automated judge
selection for the two automated non-M2 dimensions: forbidden_claim and
route_correctness. M2 (critical) and uncertainty remain human-reviewed
(V2.6/V2.12 lanes, unchanged).

## Lineage
- Predecessors (SHA-pinned in TASK-LOCK.json): V2.11 terminal
  V2_11_NO_NON_M2_JUDGE_QUALIFIES; V2.12 terminal COMPLETE/FROZEN.
- judge_core_v2_13.py is a byte-identical copy of frozen V2.11 core
  (transport, deterministic mapper, evidence-span validation).

## Frozen protocol
1. Corpus: 160 entirely new fixtures (80 forbidden / 80 route), generated by
   gen_fixtures_v2_13.py. Gold verdicts derive through the runtime
   deterministic mapper (judge_core_v2_13.derive_final). Collision audit vs
   all historical evaluation JSON corpora: 0 collisions (1314 files scanned).
2. Prepass: frozen V2.10 _expected_path rules -> 72 DETERMINISTIC_RESOLVED
   (zero judge calls, verdict=gold) + 88 EXPECTED_SEMANTIC_RESIDUAL (judge).
3. Candidates (frozen order): deepseek-v4.1-flash, mimo-v2.5-pro, ling-3.0-
   flash-sante, laguna-s-2.1. No LongCat, no GPT-5.5, no new candidates.
4. Gates (preregistered, eval_gates_v2_13.py): combined overall/forbidden/
   route >= 0.97; residual overall/forbidden/route >= 0.93; safety forbidden
   FN = 0; valid >= 0.99; evidence validity 1.0; deterministic overrides 0.
5. Stability (qualifiers only): first 10 + last 10 sorted residual ids per
   dimension (40 rows) x 3 runs, >= 0.95.
6. Selection: exactly one, sorted by combined overall, residual overall,
   p95 residual latency, wire id. No best-of-bad.

## Execution rules
- Per-row checkpointing: resume never reruns a recorded row.
- One technical retry per call inside judge_core; transport failures are
  recorded, never patched.
- No candidate calls before gold/prepass freeze; no result-driven reruns;
  no gate lowering; no writes to historical lineages.

## Terminal statuses
- V2_13_FORBIDDEN_ROUTE_JUDGE_SELECTED_FOR_FRESH_VALIDATION -> Stage 2E
  (V2.14 fresh validation, pre-authorized).
- V2_13_NO_FORBIDDEN_ROUTE_JUDGE_QUALIFIES -> Stage 2F (V2.15 human fallback,
  pre-authorized).
